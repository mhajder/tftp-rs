[package]
name = "tftp-rs"
version = "0.3.2"
edition = "2024"
description = "A high-performance TFTP server with a TUI dashboard"
license = "MIT"
authors = ["Mateusz Hajder"]
readme = "README.md"
repository = "https://github.com/mhajder/tftp-rs"
keywords = ["TFTP", "server", "TUI", "dashboard", "Rust"]
categories = ["network-programming", "command-line-utilities"]

[lib]
name = "tftp_rs"
path = "src/lib.rs"

[features]
default = ["tui"]
tui = [
    "dep:axum",
    "dep:clap",
    "dep:crossterm",
    "dep:if-addrs",
    "dep:ratatui",
    "dep:tokio-util",
]

[[bin]]
name = "tftp-rs"
path = "src/main.rs"
required-features = ["tui"]

[dependencies]
anyhow = "1.0.104"
axum = { version = "0.8.9", optional = true }
clap = { version = "4.6.6", features = ["derive"], optional = true }
crossterm = { version = "0.29", optional = true }
if-addrs = { version = "0.15", optional = true }
ratatui = { version = "0.30", optional = true }
socket2 = { version = "0.6.5", features = ["all"] }
tokio = { version = "1.53.1", features = ["fs", "io-util", "macros", "net", "rt", "rt-multi-thread", "sync", "time"] }
tokio-util = { version = "0.7.19", features = ["io"], optional = true }

[target.'cfg(any(target_os = "linux", target_os = "macos"))'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"
